import 'package:flutter/material.dart';

// Colors:
const whiteColor = Colors.white;
const textColor = Color(0xFF7950f2);
Color dialogBgColor = Colors.black12.withOpacity(0.7);
// Duration:
const transitionDuration = Duration(milliseconds: 200);
